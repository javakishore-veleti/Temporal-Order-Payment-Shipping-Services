server:
  port: 7172

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?current_schema=payment_db
    username: temporal
    password: temporal
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        hbm2ddl:
          create_namespaces=true
        format_sql: true
    sql:
      init:
        mode: always

